import {useDispatch} from "react-redux";
import {useEffect} from "react";

export const useGetCalculations = (id: string) => {

  const dispatch = useDispatch();

  // const calculatedValue = useMemo(() => getNameById(id), [id])

  return ({
    // handleSearch,
    // calculatedValue
    // loadUsers
  })
}